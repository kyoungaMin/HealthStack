# 개발일지 — 2026-02-27

## 주요 작업 내용

### 1. 설정(Settings) 시스템 구현
- **백엔드**: `app/api/v1/endpoints/settings.py`, `app/services/settings_service.py`, `app/schemas/settings.py`
- **프론트엔드**: `frontend/src/components/settings/SettingsPage.tsx`, `frontend/src/services/settingsApi.ts`
- 사용자 프로필(이름, 생년, 성별, 신장, 체중, 알레르기, 질환) 및 디스플레이 설정(테마, 폰트, TTS 속도) 관리
- Supabase `user_profiles` / `user_preferences` 테이블 활용

### 2. 웹 푸시 알림 (Web Push Notification)
- **백엔드**: `app/api/v1/endpoints/push.py`, `app/services/push_service.py`, `app/services/push_scheduler.py`
- **프론트엔드**: `frontend/src/services/pushService.ts`, `frontend/src/services/notificationService.ts`
- VAPID 키 기반 Web Push 구독/해지
- APScheduler 기반 복약 알림 스케줄러 (60초 주기)
- PWA 지원: `manifest.json`, `sw.js` 추가

### 3. 복약 스케줄 관리
- **백엔드**: `app/api/v1/endpoints/medication_schedules.py`
- **프론트엔드**: `frontend/src/components/analysis/MedicationReminderSetup.tsx`, `frontend/src/services/medicationSyncApi.ts`
- 약물별 복약 시간/요일 설정, 서버 동기화

### 4. 가족 공유 (Family Sharing)
- **백엔드**: `app/api/v1/endpoints/family.py`, `app/services/family_service.py`
- **프론트엔드**: `frontend/src/components/family/`, `frontend/src/services/familyApi.ts`
- 가족 그룹 생성/초대/가입, 가족 처방전 공유, 새 처방전 알림
- Supabase 마이그레이션: `20260227000000_family_sharing.sql`

### 5. 처방전 데이터 영구 저장 (Health Stacks Persistence)
- **백엔드**: `app/api/v1/endpoints/stacks.py` (GET/POST/DELETE)
- **프론트엔드**: `frontend/src/services/stacksApi.ts`
- 기존 localStorage 전용 → Supabase `user_health_stacks` 테이블에 영구 저장
- 로그인 시 서버에서 자동 복원, 분석 완료 시 자동 저장
- Supabase 마이그레이션: `20260227200000_health_stacks.sql`

### 6. 식단 가이드 모달
- **프론트엔드**: `frontend/src/components/analysis/DietGuideModal.tsx` (추정)
- 동의보감 약선 음식 기반 맞춤 식단 추천 UI

---

## 버그 수정

### Settings PUT 500 에러 수정
- **원인**: `user_preferences` 테이블에 `user_id` UNIQUE 제약 조건 누락 → `upsert(on_conflict="user_id")` 실패
- **수정**: `20260227100000_fix_user_preferences_unique.sql` 마이그레이션 추가

### Push 알림 NotAllowedError 수정
- **원인**: `Notification.requestPermission()` 호출 없이 `pushManager.subscribe()` 직접 호출
- **수정**: `pushService.ts`에 권한 요청 로직 선행 추가

---

## Supabase 마이그레이션 목록 (신규)
| 파일 | 내용 |
|------|------|
| `20260224000000_add_settings_columns.sql` | user_profiles/preferences 컬럼 추가 |
| `20260226000000_web_push_and_schedules.sql` | 웹 푸시 구독 및 스케줄 테이블 |
| `20260227000000_family_sharing.sql` | 가족 그룹/멤버/공유 테이블 |
| `20260227100000_fix_user_preferences_unique.sql` | user_preferences.user_id UNIQUE 제약 |
| `20260227200000_health_stacks.sql` | 건강 스택 영구 저장 테이블 |

---

## 기술 스택 변경
- `requirements.txt`: `pywebpush`, `apscheduler` 추가
- PWA 지원: Service Worker, Web App Manifest 추가
- 프론트엔드 Context: `SettingsContext` 추가

---

## 다음 작업 예정
- 처방전 데이터 서버 마이그레이션 (기존 localStorage 데이터 일괄 업로드 기능)
- 푸시 알림 안정화 및 iOS Safari 대응
- 가족 공유 UI/UX 개선
